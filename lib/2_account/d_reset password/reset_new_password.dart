import 'package:flutter/material.dart';

class reset_new_password extends StatefulWidget {
  const reset_new_password({Key? key}) : super(key: key);

  @override
  State<reset_new_password> createState() => _reset_new_passwordState();
}

class _reset_new_passwordState extends State<reset_new_password> {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
