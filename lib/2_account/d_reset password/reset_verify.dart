import 'package:flutter/material.dart';

class reset_verify extends StatefulWidget {
  const reset_verify({Key? key}) : super(key: key);

  @override
  State<reset_verify> createState() => _reset_verifyState();
}

class _reset_verifyState extends State<reset_verify> {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
